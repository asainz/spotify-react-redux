import app from './app';
import searchBox from './search-box';
import {routerReducer} from 'react-router-redux';

export default {
    app: app,
    searchBox: searchBox,
    routing: routerReducer
};
